-- `source_path` was already added and back-filled by 0004. Drizzle regenerated
-- it because the schema file was edited afterwards; this keeps the migration
-- chain consistent without touching data.
SELECT 1;
